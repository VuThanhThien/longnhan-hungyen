## ============================================
## Admin app environment
## Copy to .env for local dev, or .env.production for Docker deploy
## ============================================

## API (server actions / server-side fetch)
API_URL=http://localhost:3001/api/v1

## Production Docker / make deploy — copy to apps/admin/.env.production
# NODE_ENV=production
# API_URL=https://api.example.com/api/v1
